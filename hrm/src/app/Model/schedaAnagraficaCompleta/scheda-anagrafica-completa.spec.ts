import { SchedaAnagraficaCompleta } from './scheda-anagrafica-completa';

describe('SchedaAnagraficaCompleta', () => {
  it('should create an instance', () => {
    expect(new SchedaAnagraficaCompleta()).toBeTruthy();
  });
});
